# Omen 64xOS

> Хобби-проект 64-битной ОС с фокусом на системное программирование: boot-chain, ядро, scheduler, memory manager, syscall ABI и собственная FS.

## Omen 64xOS v0.1 — технический срез текущего состояния

## 1) Boot chain и ранний bring-up

- **Stage1 (BIOS boot sector)** загружает второй этап по LBA.
- **Stage2 loader**:
  - загружает ядро в память,
  - поднимает paging,
  - выполняет переход в **x86_64 long mode**,
  - формирует и передаёт ядру структуру boot-информации.
- **Kernel entry**:
  - инициализирует стек,
  - очищает BSS,
  - передаёт управление в основной путь инициализации.

## 2) Ядро и execution model

- Монолитное ядро под x86_64 с явным разделением платформенных и подсистемных частей.
- Базовая обработка исключений/IRQ и системный цикл инициализации.
- Наличие служебной диагностики: debug output, маркеры состояния, трассировочные точки для анализа инцидентов.

## 3) Потоки, процессы, планирование

- **Планировщик потоков** с базовыми состояниями:
  - `Ready`, `Running`, `Blocked`, `Terminated`.
- Поддерживаются:
  - принудительное вытеснение (**preemption**),
  - добровольная передача CPU (**yield**),
  - блокировки с таймаутами (**sleep/wait timeout**).
- Есть жизненный цикл объектов:
  - создание/запуск потока,
  - перевод в ожидание,
  - пробуждение,
  - завершение,
  - deferred cleanup через reaper-пути.
- Синхронизация и ожидание:
  - примитивы ожидания и сигнальные объекты,
  - базовые проверки корректности на путях wait/wake,
  - контроль конфликтов при завершении/очистке.

## 4) Подсистема памяти

- **PMM**: инициализация физической памяти из данных загрузки.
- **VMM/Paging**:
  - настройка таблиц страниц,
  - маппинг/unmap виртуальной памяти,
  - базовый контроль page permissions.
- Реализованы базовые защитные механизмы:
  - guard-page подходы,
  - контроль доступа на уровне страниц,
  - фундамент для разделения kernel/user address spaces.
- Есть задел для user-space memory semantics:
  - region-based управление,
  - подготовка к более полной модели user VM.

## 5) Syscall ABI и kernel API

- Реализован x86_64 syscall entry + dispatcher.
- Есть канонический путь kernel↔user для вызовов системных сервисов.
- Покрыты базовые группы API:
  - потоки/процессы,
  - память,
  - синхронизация,
  - операции с объектами/ресурсами.
- ABI уже отделён логически от внутренней реализации ядра, что позволяет эволюцию интерфейса без полного рефакторинга ядра.

## 6) Файловая система OMFS (v0.1)

- Собственная файловая система с **NTFS-подобной** моделью метаданных.
- Базовые on-disk сущности:
  - загрузочная запись тома,
  - таблица файловых записей,
  - bitmap-учёт аллокации,
  - журнал транзакций.
- Есть рабочая транзакционная основа:
  - write-ahead logging (redo-oriented),
  - recovery path при монтировании.
- Поддерживается базовая файловая функциональность:
  - создание/чтение/запись,
  - каталоги и перечисление,
  - основы контроля целостности метаданных.

## 7) Наблюдаемость и инженерная дисциплина

- Встроенная диагностика в критических путях ядра и FS.
- Локальные тесты и стресс-сценарии для проверки регрессий.
- Скриптовая инфраструктура сборки/запуска/отладки.
- Подход разработки: сначала корректность и устойчивость, затем микрооптимизация hot-path.

---

## Что активно дорабатывается после v0.1

- Снижение contention и задержек в горячих участках scheduler/FS.
- Усиление длительной стабильности на длинных stress-run профилях.
- Расширение user-mode совместимости и ABI-поверхности.
- Дальнейшее усиление recovery/acceptance-критериев перед следующим релизным этапом.

---

### 📈 Технические результаты
- Throughput: ...
- Latency (p95/p99): ...
- Stability/Recovery: ...

